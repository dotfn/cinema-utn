<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.grupo2.cinemautn.controllers.DashboardController.DashboardController"
            prefWidth="900" prefHeight="650">

    <BorderPane AnchorPane.topAnchor="0.0" AnchorPane.leftAnchor="0.0"
                AnchorPane.rightAnchor="0.0" AnchorPane.bottomAnchor="0.0">

        <top>
            <ToolBar style="-fx-padding: 6 12; -fx-background-color: linear-gradient(#ffffff, #e6e6e6);">
                <Label text="📽 Cinema-utn" style="-fx-font-size:18px; -fx-font-weight: bold;" />
                <Pane HBox.hgrow="ALWAYS" />
                <TextField fx:id="searchField" promptText="Buscar por título..." prefWidth="220" />
                <Button text="Buscar" onAction="#filterOnSearch" />
                <Separator orientation="VERTICAL" />
                <Button text="Perfil" onAction="#onUserProfile"/>
            </ToolBar>
        </top>

        <left>
            <VBox spacing="10" style="-fx-padding:12;" prefWidth="160">
                <Label text="Navegación" style="-fx-font-weight: bold;" />
                <Button text="Peliculas" maxWidth="Infinity" />
                <Button text="Series" maxWidth="Infinity" />
                <Region VBox.vgrow="ALWAYS" />
                <Button text="Logout" maxWidth="Infinity" />
            </VBox>
        </left>

        <center>
            <VBox spacing="12" style="-fx-padding:20;">
                <!-- Header detalle -->
                <HBox spacing="16" alignment="CENTER_LEFT" style="-fx-background-color: linear-gradient(#ffffff, #fbfbfb); -fx-padding:12; -fx-border-color:#e0e0e0; -fx-border-width:0 0 1 0;">
                    <ImageView fx:id="imgPoster" fitWidth="140" fitHeight="200" preserveRatio="true" pickOnBounds="true"/>

                    <VBox spacing="6">
                        <Label fx:id="lblTitulo" text="Título de la película" style="-fx-font-size:18px; -fx-font-weight:bold;" />
                        <HBox spacing="8" alignment="CENTER_LEFT">
                            <Label text="Director:" style="-fx-font-weight:bold;" />
                            <Label fx:id="lblDirector" text="Nombre del director" />
                            <Separator orientation="VERTICAL" prefHeight="14" />
                            <Label text="Rating:" style="-fx-font-weight:bold;" />
                            <Label fx:id="lblRating" text="-" />
                            <Label text="·" />
                            <Label fx:id="lblDuracion" text="-" />
                        </HBox>

                        <Label text="Sinopsis:" style="-fx-font-weight:bold; -fx-padding:6 0 0 0;" />
                        <TextArea fx:id="txtDescripcionHeader" prefRowCount="4" wrapText="true" editable="false" style="-fx-background-color:transparent; -fx-control-inner-background:transparent; -fx-padding:0;" />

                        <HBox spacing="8" style="-fx-padding:6 0 0 0;">
                            <Button text="Ver detalles" onAction="#onVerDetalles" />
                            <Button text="Reproducir trailer" onAction="#onReproducirTrailer" />
                        </HBox>
                    </VBox>

                    <Pane HBox.hgrow="ALWAYS" />
                    <VBox alignment="CENTER_RIGHT" spacing="8">
                        <Button minWidth="100" text="Editar" onAction="#onEditar" />
                        <Button minWidth="100" text="Eliminar" onAction="#onEliminar" />
                    </VBox>
                </HBox>

                <!-- Tabla y controles -->
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <VBox>
                        <Label text="Películas" style="-fx-font-size:16px; -fx-font-weight:bold;" />
                        <Label text="Selecciona una fila para ver el detalle arriba." style="-fx-font-size:11px; -fx-text-fill:#666;" />
                    </VBox>
                    <Pane HBox.hgrow="ALWAYS" />
                    <HBox spacing="8">
                        <Button text="Agregar" onAction="#onAgregar" />
                    </HBox>
                </HBox>

                <TableView fx:id="tableView" prefHeight="300.0" prefWidth="760.0" >
                    <columns>
                        <TableColumn fx:id="colNombre" text="Nombre" prefWidth="240.0" />
                        <TableColumn fx:id="colDescription" text="Descripción" prefWidth="420.0" />
                        <TableColumn fx:id="colDirector" text="Director" prefWidth="200.0" />
                    </columns>
                    <placeholder>
                        <Label text="No hay datos. Haz clic en 'Agregar' para crear filas." />
                    </placeholder>
                </TableView>

            </VBox>
        </center>

        <bottom>
            <HBox style="-fx-padding:8 12; -fx-background-color:#f4f4f4;" alignment="CENTER_LEFT" spacing="10">
                <Label text="Estado:" style="-fx-font-weight:bold;" />
                <Label fx:id="statusLabel" text="Listo" />
            </HBox>
        </bottom>

    </BorderPane>
</AnchorPane>
